files:

- dir_base: "env"
  src: ".env"
  dest: ".env"
  root: true

{% if (credentials.vault.encrypted_value | default('')) != '' 
%}templates:

- dir_base: "templates"
  src: "vault.tpl"
  dest: "vault"
  root: true
  params: 
    vault: "{{ credentials.vault.encrypted_value }}"
    
{% endif %}
