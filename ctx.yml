files:

- dir_base: "env"
  src: ".env"
  dest: ".env"
  root: true

{% if (params.credentials.vault.encrypted_value | default('')) != '' 
%}templates:

- dir_base: "templates"
  src: "vault.tpl"
  dest: "vault"
  root: true
  params: 
    vault: "{{ params.credentials.vault.encrypted_value }}"

{% endif %}
