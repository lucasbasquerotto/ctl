files:

- dir_base: "env"
  src: ".env"
  dest: ".env"
  root: true

templates:

{% if (credentials.vault.encrypted_value | default('')) != '' 
%}- dir_base: "templates"
  src: "vault.tpl"
  dest: "vault"
  root: true
  params: 
    vault: "{{ credentials.vault.encrypted_value }}"
{% endif %}
