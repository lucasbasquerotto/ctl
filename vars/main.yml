repo_base_dest: "/home/main/repos"

main_repo: "https://github.com/lucasbasquerotto/ansible-docker.git"
main_repo_version: "HEAD"
main_repo_dest: "{{ repo_base_dest }}/main"

repos:
- name: "client1 - repo1 - discourse forum"
  platform: "discourse"
  env_file: "env-forum.yml"
  source: "https://github.com/lucasbasquerotto/ansible-env-demo.git"
  version: "HEAD"
  dest: "{{ repo_base_dest }}/clients/client1/repo1"